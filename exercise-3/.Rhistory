number + 3
return(number + 3)
return(x + 3)
return(result)
add_three <- function(x) {
result <- x+3
return(result)
}
add_three <- function(x) {
return(x + 3)
}
rm(add_three())
add_threer <- function(x) {
return(x+3)
}
add_three(3)
# Create a variable `ten` that is the result of passing 7 to your `add_three`
# function
ten <- add_three(7)
imperial_to_metric <- (x, y) {
total_meter <- x*0.3048 + y*0.0254
return(total_meter)
}
imperial_to_metric <- (x, y) {
total_meter <- x*0.3048 + y*0.0254
return(total_meter)
}
imperial_to_metric <- (x, y) {
total_meter <- (x*0.3048 + y*0.0254)
return(total_meter)
}
imperial_to_metric <- function(x, y) {
total_meter <- (x*0.3048 + y*0.0254)
return(total_meter)
}
height_in_meters <- imperial_to_metric(5,7.2)
